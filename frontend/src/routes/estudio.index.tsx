import { createFileRoute } from "@tanstack/react-router";
import { StudioLanding } from "@/features/creation/creation-studio";
export const Route = createFileRoute("/estudio/")({ component: StudioLanding });
