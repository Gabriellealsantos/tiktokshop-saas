import { createFileRoute } from "@tanstack/react-router";
import { ReferralScreen } from "@/features/misc/misc-screens";
export const Route = createFileRoute("/indicacao")({ component: ReferralScreen });
